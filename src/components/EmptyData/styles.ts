import styled from 'styled-components';

import { fontSizes, space } from '@/styles/themes/general';

export const Title = styled.h2`
  font-size: ${fontSizes[4]};
  margin: ${space[4]} auto;
`;

export const Description = styled.p`
  font-size: ${fontSizes[2]};
`;
